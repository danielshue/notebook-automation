mode: ContinuousDelivery
next-version: 0.1.0-beta.6
branches:
  master:
    regex: ^master$
    mode: ContinuousDelivery
    label: ''
  main:
    regex: ^main$
    mode: ContinuousDelivery
    label: ''
  develop:
    regex: ^develop$
    mode: ContinuousDelivery
    label: 'beta'
